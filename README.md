# Usage

1. Start `tmux`
2. Edit the runtime config block at the top of `two-agent-metaprompt.md`
3. Open your A0 runtime in the project root (for example `claude` or `codex`)
4. Copy and send this prompt
5. Communicate with **A0 only** (your original pane), A1 agent will be controlled by A0.
